# 🚀 Быстрое добавление вопросов из статьи

## Проблема
Статья coko1.ru с топ-25 вопросов блокирует автоматическую загрузку → нужно добавить вручную.

## Решение (выберите один способ)

### ⚡ Способ 1: Интерактивный (самый простой)

```bash
# 1. Откройте статью в браузере
# 2. Запустите скрипт
python scripts/add_questions_to_dataset.py --interactive

# 3. Копируйте вопросы из статьи и вставляйте в терминал
# 4. Enter x2 для сохранения каждого вопроса
# 5. Пустой вопрос для выхода
```

### 📦 Способ 2: Batch (для массового добавления)

```bash
# 1. Скопируйте все 25 вопросов из статьи в JSON
nano scripts/my_questions.json
```

Формат JSON:
```json
[
  {
    "question": "Вопрос из статьи?",
    "ground_truth": "Ответ эксперта с деталями..."
  },
  {
    "question": "Следующий вопрос?",
    "ground_truth": "Следующий ответ..."
  }
]
```

```bash
# 2. Импортируйте
python scripts/add_questions_to_dataset.py --input scripts/my_questions.json
```

## Проверка

```bash
# Посмотреть сколько вопросов
wc -l tests/dataset.csv

# Показать все вопросы
python scripts/add_questions_to_dataset.py --show
```

## Цель
- Сейчас: 16 вопросов
- Минимум: 50 вопросов
- Оптимально: 100 вопросов

## Следующий шаг
После добавления вопросов:
```bash
# Запустить eval на новом датасете
python eval/run_full_evaluation.py
```

---

📖 Подробная инструкция: `docs/ADDING_QUESTIONS_FROM_ARTICLE.md`
